describe('Main', () => {
  describe('Method A', () => {
    context('Case 1', () => {
      it('should happen blabla', () => {
        // espera que aconteça
        // Entra de dados / métodos sum(2,2)
        // Espera retornar (4) => true | (3) => false => broken test
      });
    });

    context('Case 2', () => {});
  });

  describe('Method B', () => {});
});
